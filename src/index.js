import React from 'react';
import ReactDOM from 'react-dom';
import { JournalApp } from './JournalApp';

import './styles/styles.scss'; //npm install node-sass

ReactDOM.render(
  
    <JournalApp />,
  
  document.getElementById('root')
);